- hosts: all
  become: true
  tasks:
    - name: create new deployment
      command: kubectl apply -f /home/main/Desktop/devops-project-1/Deployment.yml
    - name: create new service
      command: kubectl apply -f /home/main/Desktop/devops-project-1/service.yml

